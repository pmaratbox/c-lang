# 0055 — Frequency Count

Count how many times each letter appears in `banana` and print the per-letter counts in alphabetical order: `a:3 b:1 n:2`. A 256-entry `int` array indexed by byte value is the tally; scanning it from `0` upward prints the present letters already in ascending byte (here alphabetical) order.

## Run

    make && ./frequency
