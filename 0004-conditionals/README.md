# 0004 — Conditionals

Compare `n` against 10 and print whether it's less, equal, or greater. C
allows omitting the braces when each branch is a single statement, but
always using them avoids classic dangling-else and "goto fail" bugs. Edit
`n` to `10` or `15` to exercise the other branches.

## Run

    make && ./conditionals
