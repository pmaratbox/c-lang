# 0337 — Multiplication Table

Print the 3x3 multiplication table, each row space-separated. Nested `for` loops with a ternary choosing space-or-newline format each row.

## Run

    make && ./multiplicationtable
