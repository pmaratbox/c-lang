# 0333 — Classify Even/Odd

Classify each of [1,2,3,4] as odd or even, printing `odd even odd even`. In C, `n % 2 == 0` with a ternary selects the label idiomatically.

## Run

    make && ./evenoddclassify
