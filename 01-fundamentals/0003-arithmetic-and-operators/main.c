#include <stdio.h>

int main(void) {
    int a = 17;
    int b = 5;

    printf("a + b: %d\n", a + b);
    printf("a - b: %d\n", a - b);
    printf("a * b: %d\n", a * b);
    printf("a / b: %d\n", a / b);
    printf("a %% b: %d\n", a % b);
    return 0;
}
