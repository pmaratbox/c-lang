# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. In C the modulo operator `%` drives the branch selection.

## Run

    make && ./fizzbuzz
