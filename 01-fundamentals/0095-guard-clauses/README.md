# 0095 — Guard Clauses

Classify each of `-1`, `0`, and `5` using guard clauses (an early return for each special case) and print `negative`, `zero`, and `positive`. Each guard returns a string literal early; the final `return` handles the remaining case.

## Run

    make && ./guard
