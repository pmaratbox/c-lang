# 0413 — Zip Streams

Implement zip that pairs values by index and combines them; zip [1,2,3] with [10,20,30] using a+b. In C, per-source values are buffered in small fixed arrays and drained pairwise via function-pointer observers.

## Run

    make -s && ./zipstreams
