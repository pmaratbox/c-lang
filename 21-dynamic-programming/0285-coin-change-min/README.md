# 0285 — Coin Change (Min Coins)

Find the fewest coins from {1,2,5} summing to 11, printing `3`. A flat `int dp[]` array filled bottom-up keeps the C idiomatic and allocation-free.

## Run

    make && ./coinchangemin
