# 0291 — Minimum Path Sum

Find the minimum path sum through [[1,3,1],[1,5,1],[4,2,1]] moving right/down, printing `7`. A rolling `dp[]` row plus the ternary `?:` for the min keeps this compact in C.

## Run

    make && ./minpathsum
