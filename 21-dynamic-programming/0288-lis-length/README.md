# 0288 — Longest Increasing Subsequence

Compute the LIS length of [10,9,2,5,3,7,101,18], printing `4`. The classic O(n^2) `dp[]` scan over a plain C array reads clearly here.

## Run

    make && ./lislength
