# 0290 — Unique Grid Paths

Count lattice paths from the top-left to bottom-right of a 3x3 grid moving right/down, printing `6`. A single rolling `dp[]` row accumulates path counts column by column.

## Run

    make && ./uniquepaths
