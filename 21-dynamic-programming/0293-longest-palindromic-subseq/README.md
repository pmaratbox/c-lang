# 0293 — Longest Palindromic Subsequence

Compute the longest palindromic subsequence length of "bbbab", printing `4`. A 2-D `dp[i][j]` table filled over substrings is the natural C encoding.

## Run

    make && ./longestpalindromicsubseq
