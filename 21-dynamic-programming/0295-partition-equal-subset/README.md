# 0295 — Equal Partition

Decide whether [1,5,11,5] can split into two equal-sum subsets, printing `yes`. The problem reduces to a subset-sum to half the total over a boolean `dp[]` in C.

## Run

    make && ./partitionequalsubset
