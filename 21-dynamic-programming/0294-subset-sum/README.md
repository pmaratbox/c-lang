# 0294 — Subset Sum

Decide whether some subset of [3,34,4,12,5,2] sums to 9, printing `yes`. A boolean `dp[]` scanned high-to-low marks reachable sums in idiomatic C.

## Run

    make && ./subsetsum
