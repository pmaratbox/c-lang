# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. A `start` flag marks the first letter after each space; `toupper` (from `<ctype.h>`) capitalizes it in place.

## Run

    make && ./titlecase
