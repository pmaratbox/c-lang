# 0381 — Parallel Map

Square 1,2,3,4 in parallel and collect the results in input order, printing `1 4 9 16`. In C each thread writes into its own indexed slot so order is preserved.

## Run

    make && ./parallelmap
