# 0379 — Future Chain

Chain asynchronous steps 5 -> (*2) -> (+1) and print the final value `11`. In C each step runs on its own thread whose joined result feeds the next.

## Run

    make && ./futurechain
