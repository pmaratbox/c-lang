#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    printf("sqrt: %d\n", (int)sqrt(16));
    printf("pow: %d\n", (int)pow(2, 10));
    printf("abs: %d\n", abs(-5));
    printf("max: %d\n", 3 > 9 ? 3 : 9);
    return 0;
}
