# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. The same ternary-based helper runs once with the flag set and once cleared.

## Run

    make && ./featureflag
