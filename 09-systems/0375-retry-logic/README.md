# 0375 — Retry With Attempts

Retry an operation that fails on attempts 1 and 2 and succeeds on attempt 3, printing `ok after 3`. A simple `for` loop retries up to N times and breaks on the first success.

## Run

    make && ./retrylogic
