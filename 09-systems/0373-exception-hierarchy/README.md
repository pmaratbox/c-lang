# 0373 — Exception Hierarchy

Throw a specific error subtype and catch it through a base-type handler, printing `caught base`. C has no exceptions, so an error is tagged with a kind and matched by its category.

## Run

    make && ./exceptionhierarchy
