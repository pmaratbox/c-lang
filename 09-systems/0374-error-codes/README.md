# 0374 — Error Codes

Map the numeric error code 2 to its message "not found" and print `not found`. A static array indexed by the code gives a direct code-to-message lookup.

## Run

    make && ./errorcodes
