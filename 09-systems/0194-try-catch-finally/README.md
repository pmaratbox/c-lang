# 0194 — Try / Catch / Finally

Throw and catch an error, printing `caught`, and always run a finally block printing `cleanup`, on two lines. C has no exceptions, so handle the error value then run an explicit cleanup step.

## Run

    make && ./catchfinally
