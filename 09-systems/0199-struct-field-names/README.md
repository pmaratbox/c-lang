# 0199 — Struct Field Names

List the field names of a Point{x,y} struct/record and print `x y`. C has no reflection, so the field-name list is declared by hand in declaration order.

## Run

    make && ./fieldnames
