# 0086 — Sort a Map by Value

Sort the map `{a: 3, b: 1, c: 2}` by value in ascending order and print the entries: `b:1 c:2 a:3`. An array of `{key, value}` structs is sorted with `qsort`, comparing the value fields.

## Run

    make && ./sortmap
