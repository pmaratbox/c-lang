# 0165 — All and Any

For [2,4,6] report whether all are even (yes) and whether any are odd (no), printing `yes no`. A single loop folds both predicates into boolean flags.

## Run

    make && ./any
