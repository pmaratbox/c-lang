# 0168 — List Difference

Compute [1,2,3,4] minus the elements of [2,4], printing `1 3`. Each element of the first list is kept only if a linear scan of the second list does not find it.

## Run

    make && ./difference
