# 0085 — Rotate a List

Rotate the list `1, 2, 3, 4, 5` left by `2` positions (elements wrap to the end) and print it: `3 4 5 1 2`. Reading index `(i + k) % n` for each output position prints the left rotation without copying the array.

## Run

    make && ./rotate
