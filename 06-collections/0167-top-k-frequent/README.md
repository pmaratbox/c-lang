# 0167 — Top-K Frequent

Find the 2 most frequent elements of [a,b,a,c,b,a], printing `a b`. Counts are tallied in an array, then sorted by count descending with first-seen order breaking ties.

## Run

    make && ./kfrequent
