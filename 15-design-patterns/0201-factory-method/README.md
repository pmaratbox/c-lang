# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. In C the factory is a plain function returning a struct selected by string comparison.

## Run

    make && ./factorymethod
