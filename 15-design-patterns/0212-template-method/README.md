# 0212 — Template Method

Run an algorithm skeleton whose middle step is overridden, printing `start work end` on one line. The fixed run() frames a caller-supplied step function pointer between the start and end text.

## Run

    make && ./templatemethod
