# 0204 — Adapter

Adapt a Celsius source (100) to a Fahrenheit interface, printing `212`. The adapter struct holds a pointer to the Celsius source and exposes a fahrenheit() conversion function.

## Run

    make && ./adapter
