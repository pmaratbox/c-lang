# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The Circle holds a pointer to a Color implementor whose name() function pointer supplies the prefix.

## Run

    make && ./bridge
