# 0207 — Composite

Sum a composite tree of leaf values 1, 2, 3 through a uniform size() interface, printing `6`. A single Node struct serves as both leaf and composite, and size() recurses over children.

## Run

    make && ./composite
