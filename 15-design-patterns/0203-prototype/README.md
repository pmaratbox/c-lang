# 0203 — Prototype (Clone)

Clone a prototype object holding 1, mutate the clone to 2, and show the original is unchanged, printing `1 2`. In C struct assignment copies by value, giving an independent clone for free.

## Run

    make && ./prototype
