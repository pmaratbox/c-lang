# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. A small struct holds the cursor and end, with has_next()/next() driving the loop.

## Run

    make && ./iteratorpattern
