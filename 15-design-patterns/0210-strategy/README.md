# 0210 — Strategy

Select an "add" then a "mul" strategy and apply each to (3,4), printing `7 12`. A function-pointer typedef is the idiomatic C strategy, swapped between two operations.

## Run

    make && ./strategy
