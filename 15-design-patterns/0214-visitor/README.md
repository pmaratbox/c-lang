# 0214 — Visitor

Use a visitor to sum the values of a small node tree with leaves 1, 2, 3, printing `6`. A visit() function walks the tree carrying an accumulator passed by pointer.

## Run

    make && ./visitor
