# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. A ThemeFactory struct carries the theme prefix and builder functions stamp it onto each widget label.

## Run

    make && ./abstractfactory
