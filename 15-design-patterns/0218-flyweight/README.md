# 0218 — Flyweight

Share flyweight instances for repeated characters a, b, a and print the number of distinct objects created `2`. A factory caches one instance per character key, so the repeated 'a' reuses the existing one.

## Run

    make && ./flyweight
