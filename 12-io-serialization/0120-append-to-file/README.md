# 0120 — Append to a File

Write "a" to a file, append "b", then read both lines back and print `a b`. C opens with mode `"w"` then `"a"`, and joins the lines with a space.

## Run

    make && ./tofile
