# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. In C the compact string is built directly with printf escaping the quotes.

## Run

    make && ./jsonstringify
