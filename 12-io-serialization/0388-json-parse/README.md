# 0388 — JSON Parse

Parse the JSON object `{"x":1,"y":2}` and print its entries `x=1 y=2`. A tiny hand-written scanner walks the string pulling quoted keys and digit runs.

## Run

    make && ./jsonparse
