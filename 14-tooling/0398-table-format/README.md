# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. The `%-*s` printf conversion left-justifies a string to a width computed at runtime.

## Run

    make && ./tableformat
