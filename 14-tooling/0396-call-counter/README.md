# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A static counter incremented by a wrapper function is the idiomatic C approach.

## Run

    make && ./callcounter
