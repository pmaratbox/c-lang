# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. An array of function pointers serves as the list of tests to drive.

## Run

    make && ./testrunner
