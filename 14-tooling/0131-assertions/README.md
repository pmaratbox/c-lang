# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. In C a tiny check helper that calls `exit(1)` on failure stands in for an assert macro.

## Run

    make && ./assertions
