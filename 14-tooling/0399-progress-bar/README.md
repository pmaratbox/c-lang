# 0399 — Progress Bar

Render a width-10 progress bar at 40% completion, printing `[####------]`. Rounding the filled count and looping over the width keeps the rendering explicit.

## Run

    make && ./progressbar
