# 0341 — Recursive List Reverse

Reverse [1,2,3] using recursion (no built-in reverse), printing `3 2 1`. In C, recurse from the tail and print each element space-separated.

## Run

    make && ./recursivelistreverse
