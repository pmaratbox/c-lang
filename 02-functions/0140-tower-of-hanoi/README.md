# 0140 — Tower of Hanoi

Count the moves needed to solve Tower of Hanoi for 3 disks recursively and print `7`. The recurrence `2 * moves(n - 1) + 1` maps cleanly onto a C function.

## Run

    make && ./ofhanoi
