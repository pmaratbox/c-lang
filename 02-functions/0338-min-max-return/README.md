# 0338 — Return Min and Max

Return both the minimum and maximum of [4,1,7] from one function and print `1 7`. In C, return both via a small struct since functions return a single value.

## Run

    make && ./minmaxreturn
