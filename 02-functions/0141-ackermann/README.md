# 0141 — Ackermann Function

Compute the Ackermann function A(2,3) recursively and print `9`. The deeply nested recursion translates directly into three C base/recursive cases.

## Run

    make && ./ackermann
