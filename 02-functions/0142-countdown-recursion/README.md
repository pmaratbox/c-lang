# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. Each C call prints its value plus a separating space before recursing.

## Run

    make && ./recursion
