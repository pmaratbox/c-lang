# 0383 — Partition Into Runs

Partition [1,1,2,3,3,3] into runs of equal adjacent elements, printing `1 1|2|3 3 3`. In C, a single index scan groups consecutive equal elements without extra allocation.

## Run

    make && ./partitionruns
