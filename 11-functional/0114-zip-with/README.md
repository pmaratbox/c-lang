# 0114 — Zip With

Combine [1,2,3] and [4,5,6] element-wise with addition, printing `5 7 9`. A `zip_with` function takes a binary function pointer and walks both arrays in lockstep.

## Run

    make && ./with
