# 0111 — Partial Application

Partially apply a two-argument add by fixing the first argument to 10, then call the result with 3 to print `13`. A `PartialAdd` struct binds the first argument, and `apply` supplies the remaining one.

## Run

    make && ./application
