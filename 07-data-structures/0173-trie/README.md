# 0173 — Trie

Insert "cat" and "car" into a trie, then search "car" (yes) and "can" (no), printing `yes no`. Each node holds a fixed `children[26]` array indexed by letter plus an `end` flag marking a complete word.

## Run

    make && ./trie
