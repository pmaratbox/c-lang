# 0171 — Doubly Linked List

Build a doubly linked list 1<->2<->3, traverse forward then backward, printing `1 2 3` then `3 2 1` on two lines. Each `Node` holds `prev` and `next` pointers so traversal works in either direction from `head` or `tail`.

## Run

    make && ./linkedlist
