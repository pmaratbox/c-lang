# 0178 — Stack With Min

Build a stack that tracks its minimum in O(1); after pushing 3,1,2 print `min: 1`. A parallel `mins` array records the running minimum at each depth, so `get_min` is a single array read.

## Run

    make && ./withmin
