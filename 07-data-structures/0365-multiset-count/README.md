# 0365 — Multiset Counts

Add 1,1,2 to a multiset; print the count of 1 (2), remove one 1, then print the count of 1 (1), as `2 1`. In C parallel key/count arrays act as the element-to-count map, where add increments and remove decrements.

## Run

    make && ./multisetcount
