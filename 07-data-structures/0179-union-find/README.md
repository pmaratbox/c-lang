# 0179 — Union-Find

Union (0,1) and (2,3), then query connectivity of (0,1)=yes and (0,2)=no, printing `yes no`. A `parent` array with `find` (using path compression) and `unite` implements a disjoint-set forest.

## Run

    make && ./find
