# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. An array stores the complete binary tree, with sift-up on `push` and sift-down on `pop` keeping the smallest element at index 0.

## Run

    make && ./heapmanual
