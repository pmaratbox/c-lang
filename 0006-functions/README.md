# 0006 — Functions

Define `add(a, b)` returning their sum, call it with `3` and `4`, and print
the result. A function must be declared before it is used — here `add` is
defined above `main`, so the compiler already knows its signature. For
functions defined later, a forward prototype (`int add(int, int);`) is
needed.

## Run

    make && ./functions
