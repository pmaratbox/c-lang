# 0005 — Loops

Print 1..5 with the classic three-part `for` loop. Declaring the counter
`int i` inside the loop header is C99+; pre-C99 required the variable to
be declared at the top of the enclosing block.

## Run

    make && ./loops
