# 0012 — Enums

Define a `Color` enum (`RED`, `GREEN`, `BLUE`) and print the integer value of
`GREEN` (1) and `BLUE` (2). A C `enum` is just a set of named `int` constants
that auto-number from 0; the names are interchangeable with their integer
values.

## Run

    make && ./enums
