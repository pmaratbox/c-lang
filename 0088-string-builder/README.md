# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. C has no builder type; you write into a `char` buffer, tracking the length (`sprintf` returns how many characters it wrote).

## Run

    make && ./stringbuilder
