# 0342 — Tagged Union Eval

Model an expression tagged union Num | Add and evaluate Add(Num 1, Num 2), printing `3`. In C a tagged union pairs an enum tag with a `union`, dispatched by `switch`.

## Run

    make && ./taggedunioneval
