# 0144 — Flag Enum (Bit Flags)

Combine bit flags READ(1) and WRITE(2) into 3, check that WRITE is set, and print `3 yes`. In C, enum constants combine with bitwise OR and test with bitwise AND.

## Run

    make && ./enum
