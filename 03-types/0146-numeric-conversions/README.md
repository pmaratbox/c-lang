# 0146 — Numeric Conversions

Truncate 3.9 to the int 3 and widen the int 3 to the float 3.0, printing `3 3.0`. In C, a cast to `int` truncates toward zero and `%.1f` formats the widened float.

## Run

    make && ./conversions
