# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. C enum constants are plain ints starting at 0, so a parallel name array recovers the label.

## Run

    make && ./enumordinal
