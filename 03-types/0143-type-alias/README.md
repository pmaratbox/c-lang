# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. In C, `typedef` creates the alias with no runtime cost.

## Run

    make && ./alias
