# 0071 — Celsius to Fahrenheit

Convert `100` and `0` degrees Celsius to Fahrenheit using `F = C * 9 / 5 + 32`, printing `100C = 212F` and `0C = 32F`. Integer arithmetic: `c * 9` is computed before the truncating `/ 5`, so order matters for the result.

## Run

    make && ./celsius
