# 0092 — Two Sum

Find the two indices in `2, 7, 11, 15` whose values sum to `9` and print them: `0 1`. Without a built-in hash map, a nested loop checks every pair — O(n^2), where a hash map would give the O(n) single-pass version.

## Run

    make && ./twosum
