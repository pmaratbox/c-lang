# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. A simple loop walks the array, stopping at the first element equal to the target.

## Run

    make && ./linsearch
