# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. Recursive backtracking over a sorted array yields lexicographic order.

## Run

    make && ./permutations
