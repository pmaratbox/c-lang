# 0091 — Balanced Parentheses

Using a stack, check whether `(())` is balanced and whether `(()` is balanced, printing `yes` then `no`. A fixed `char` array with a `top` index is the stack; `(` pushes and `)` pops, with an empty `top` at the end meaning balanced.

## Run

    make && ./balanced
