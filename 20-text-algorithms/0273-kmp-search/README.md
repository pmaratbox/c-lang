# 0273 — KMP Search

Use Knuth-Morris-Pratt to find all start indices of "ab" in "ababab", printing `0 2 4`. Build the prefix-function array on the stack and scan in a single pass.

## Run

    make && ./kmpsearch
