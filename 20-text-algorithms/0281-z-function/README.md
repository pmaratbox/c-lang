# 0281 — Z-Function

Compute the Z-array of "aaaa" (excluding z[0]) and print `3 2 1`. The Z-box `[l,r)` is maintained to reuse previously matched prefixes in linear time.

## Run

    make && ./zfunction
