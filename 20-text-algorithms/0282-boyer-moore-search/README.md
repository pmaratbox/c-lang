# 0282 — Boyer-Moore Search

Use the bad-character rule to find "abc" in "zzabc", printing the index `2`. A 256-entry last-occurrence table drives the bad-character shift.

## Run

    make && ./boyermooresearch
