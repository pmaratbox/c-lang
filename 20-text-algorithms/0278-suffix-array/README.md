# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. Sort start indices with `qsort` keyed by `strcmp` on each suffix.

## Run

    make && ./suffixarray
