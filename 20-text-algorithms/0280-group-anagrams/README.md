# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. Each word is keyed by its sorted letters and matched against existing keys with `strcmp`.

## Run

    make && ./groupanagrams
