# 0277 — Longest Palindromic Substring

Find the longest palindromic substring of "babad" (first of maximal length), printing `bab`. Expand around each odd and even center, keeping the earliest maximal run.

## Run

    make && ./longestpalindromesubstring
