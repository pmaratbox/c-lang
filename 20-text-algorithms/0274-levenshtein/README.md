# 0274 — Levenshtein Distance

Compute the edit distance between "kitten" and "sitting", printing `3`. A two-dimensional DP table on the stack tracks insert, delete, and substitute costs.

## Run

    make && ./levenshtein
