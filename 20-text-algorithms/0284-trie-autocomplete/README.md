# 0284 — Trie Autocomplete

Insert "car","card","dog" into a trie and autocomplete the prefix "car", printing `car card`. A fixed node pool backs the 26-way trie and a DFS collects words in lexicographic order.

## Run

    make && ./trieautocomplete
