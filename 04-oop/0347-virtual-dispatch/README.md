# 0347 — Virtual Dispatch

Store a Circle, Square, and Triangle in one Shape collection and call describe() on each, printing `circle square triangle`. In C, a struct holding a function pointer simulates a vtable for dynamic dispatch.

## Run

    make && ./virtualdispatch
