# 0151 — Singleton

Obtain a singleton instance twice and confirm both references are the same object, printing `same: yes`. In C, a function-local `static` gives a single lazily-stored instance whose address is shared.

## Run

    make && ./singleton
