# 0350 — Static Factory Method

Use a static factory Color.fromHex("#ff0000") to build a color and print its components `255 0 0`. In C, a file-`static` constructor function returns a fully-built struct by value.

## Run

    make && ./staticfactory
