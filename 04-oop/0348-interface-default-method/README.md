# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. In C, an interface is a function-pointer struct whose default can be swapped for an override.

## Run

    make && ./interfacedefaultmethod
