# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. In C, a file-`static` variable plays the role of a class-level counter incremented by the constructor function.

## Run

    make && ./instancecounter
