# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. In C, encapsulation is conventional: the struct field is mutated only through dedicated functions.

## Run

    make && ./encapsulation
