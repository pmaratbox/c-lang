# 0154 — Polymorphic Area

Sum the areas of a Rectangle(2,3)=6 and a Triangle(base=4,height=4)=8 through a common Shape interface, printing `total area: 14`. In C, polymorphism is achieved with a struct of function pointers embedded as the first member of each concrete type.

## Run

    make && ./area
