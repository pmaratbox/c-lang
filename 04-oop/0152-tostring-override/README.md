# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. In C, the idiom is a dedicated function that formats the struct into a caller-supplied buffer with snprintf.

## Run

    make && ./override
