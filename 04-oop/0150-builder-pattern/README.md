# 0150 — Builder Pattern

Use a fluent builder to construct a Pizza of size M with cheese, and print `Pizza(M, cheese)`. In C, the fluent chain is built by having each builder function return the builder pointer.

## Run

    make && ./pattern
