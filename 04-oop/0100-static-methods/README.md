# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. C has no classes; a plain function is the equivalent of a static method.

## Run

    make && ./staticmethods
