# 0153 — Comparable Sort

Sort people [(alice,30),(bob,25)] by age ascending and print their names `bob alice`. In C, ordering is supplied as a comparator function passed to qsort.

## Run

    make && ./sort
