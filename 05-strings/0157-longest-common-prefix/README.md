# 0157 — Longest Common Prefix

Find the longest common prefix of ["flower","flow","flight"], printing `fl`. Walk column by column over the array of `char *` until a mismatch or string end.

## Run

    make && ./commonprefix
