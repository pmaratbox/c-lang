# 0161 — Remove Duplicate Characters

Remove duplicate characters from "aabbcc" keeping first occurrence, printing `abc`. Track seen bytes in a boolean array and emit each character only the first time.

## Run

    make && ./duplicatechars
