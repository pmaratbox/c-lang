# 0159 — String Interpolation

Interpolate name="Ada" and age=36 into a sentence, printing `Ada is 36`. C has no string interpolation, so use `printf` format specifiers.

## Run

    make && ./interpolation
