# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. In C a character literal `'A'` is already an `int`; `%d` prints its code and `%c` prints the character for a code.

## Run

    make && ./charcodes
