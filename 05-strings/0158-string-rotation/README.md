# 0158 — String Rotation Check

Check whether "cdab" is a rotation of "abcd", printing `yes`. A string is a rotation iff it has the same length and is a substring of the original concatenated with itself.

## Run

    make && ./rotation
