# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `strstr` returns a pointer to the match; subtracting the base pointer gives the index (it would be `NULL` if absent).

## Run

    make && ./substring
