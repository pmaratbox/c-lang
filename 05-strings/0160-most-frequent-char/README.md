# 0160 — Most Frequent Character

Find the most frequent character in "hello", printing `l`. Tally counts in a 256-entry array and scan in original order to keep the first-seen winner on ties.

## Run

    make && ./frequentchar
