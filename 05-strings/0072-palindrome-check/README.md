# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. Two indices walk in from the ends, comparing bytes; the first mismatch returns false. Correct for ASCII.

## Run

    make && ./palindrome
