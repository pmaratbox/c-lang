# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. `strchr` returns a non-null pointer when the character is a vowel; the loop characters are never the terminator, so the test is safe.

## Run

    make && ./vowels
