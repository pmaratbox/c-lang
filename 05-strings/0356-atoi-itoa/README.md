# 0356 — String<->Int Round Trip

Parse "-42" to the integer -42 and format it back to "-42", printing `-42 -42`. In C, hand-roll atoi via digit accumulation and itoa via reversed digit extraction.

## Run

    make && ./atoiitoa
