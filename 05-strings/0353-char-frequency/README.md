# 0353 — Character Frequency

Count character frequencies in "aab" and print them in first-seen order `a:2 b:1`. In C, track first-appearance order with a parallel array of seen characters.

## Run

    make && ./charfrequency
