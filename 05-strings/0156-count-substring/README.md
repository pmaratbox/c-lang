# 0156 — Count Substring Occurrences

Count non-overlapping occurrences of "ab" in "ababab", printing `3`. Use `strstr` and advance past each match by the needle length.

## Run

    make && ./substring
