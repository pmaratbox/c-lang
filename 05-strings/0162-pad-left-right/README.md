# 0162 — Pad Left and Right

Left-pad and right-pad "5" with spaces to width 3 and print both joined by a pipe: `  5|5  `. Use `printf` field widths: `%3s` right-aligns and `%-3s` left-aligns.

## Run

    make && ./leftright
