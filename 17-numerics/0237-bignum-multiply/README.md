# 0237 — Big Number Multiplication

Multiply 123 by 456 with schoolbook long multiplication on digit strings, printing `56088`. In C we accumulate partial products into an int array indexed by digit position, normalizing carries.

## Run

    make && ./bignummultiply
