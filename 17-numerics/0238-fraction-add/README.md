# 0238 — Fraction Addition

Add the fractions 1/2 and 1/3 and print the reduced result `5/6`. In C we cross-multiply over a common denominator then divide both parts by their gcd.

## Run

    make && ./fractionadd
