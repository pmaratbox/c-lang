# 0249 — Binomial Coefficient

Compute C(5,2) without overflow and print `10`. In C we use the multiplicative formula, dividing at each step so intermediates stay small.

## Run

    make && ./binomialcoefficient
