# 0321 — Lazy Take

Build a lazy sequence of the natural numbers and take the first five, printing `1 2 3 4 5`. C has no generators, so a small stateful struct yields each natural on demand.

## Run

    make && ./lazytake
