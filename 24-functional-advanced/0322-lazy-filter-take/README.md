# 0322 — Lazy Filter + Take

From a lazy stream of naturals, filter the even ones and take three, printing `2 4 6`. The filter is a pull that keeps advancing the stateful naturals until an even value appears.

## Run

    make && ./lazyfiltertake
