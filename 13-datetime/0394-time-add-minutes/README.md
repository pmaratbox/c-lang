# 0394 — Add Minutes to Time

Add 90 minutes to 10:45 and print the resulting 24-hour time `12:15`. In C we use `printf` with `%02d` to zero-pad the hours and minutes.

## Run

    make && ./timeaddminutes
