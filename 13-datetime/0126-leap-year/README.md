# 0126 — Leap Year Check

Determine whether 2000, 1900, and 2024 are leap years, printing `yes no yes`. In C a small static helper returns an int used directly in a ternary.

## Run

    make && ./year
