# 0393 — Weekday After N Days

Given 2000-01-01 is a Saturday, compute the weekday 3 days later, printing `Tuesday`. In C we index a static array of weekday names with modular arithmetic.

## Run

    make && ./weekdayafterdays
