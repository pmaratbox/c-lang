# 0311 — BST Delete

Insert 5,3,8,1,4 into a BST, delete 3, and print the in-order traversal `1 4 5 8`. The C version replaces a two-child node with its in-order successor and `free`s the removed node.

## Run

    make && ./bstdelete
