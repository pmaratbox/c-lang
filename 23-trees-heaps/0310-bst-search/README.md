# 0310 — BST Search

Insert 5,3,8,1,4 into a BST, then search for 4 (yes) and 6 (no), printing `yes no`. The C lookup is an iterative `while` descent over heap-allocated nodes.

## Run

    make && ./bstsearch
