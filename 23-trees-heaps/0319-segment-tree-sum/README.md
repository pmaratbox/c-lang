# 0319 — Segment Tree Range Sum

Build a segment tree over [1,2,3,4,5] and query the sum of indices 1..3 (inclusive), printing `9`. The C tree lives in a flat `4*N` array indexed with the `2*node` / `2*node+1` child convention.

## Run

    make && ./segmenttreesum
