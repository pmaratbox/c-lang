# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. The C heap is a plain array with sift-up on push and sift-down on pop.

## Run

    make && ./maxheappop
