# 0312 — Tree Height

Compute the height (in nodes) of the BST built from 5,3,8,1,4, printing `3`. In C the recursion returns `1 + max(left, right)` with a NULL subtree counted as 0.

## Run

    make && ./treeheight
