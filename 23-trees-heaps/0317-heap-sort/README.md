# 0317 — Heap Sort

Heap-sort [5,3,8,1,4] ascending, printing `1 3 4 5 8`. The C version sorts the array in place: build a max-heap, then swap the root to the end and sift down the shrinking heap.

## Run

    make && ./heapsort
