# 0318 — Kth Largest

Find the 2nd largest element of [3,2,1,5,6,4] using a heap, printing `5`. The C version keeps a fixed size-2 min-heap as an array; its root is the kth largest.

## Run

    make && ./kthlargest
