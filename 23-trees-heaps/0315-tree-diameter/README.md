# 0315 — Tree Diameter

Compute the diameter (longest path in edges) of a tree where root has children A and B, and A has children C and D, printing `3`. In C a single recursive `height` updates a file-scope `best` with `left + right` at each node.

## Run

    make && ./treediameter
