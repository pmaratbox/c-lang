# 0313 — Level-Order Traversal

Print the breadth-first (level-order) traversal of the BST built from 5,3,8,1,4 `5 3 8 1 4`. The C BFS uses a fixed-size array as a ring-free queue with head/tail indices.

## Run

    make && ./levelordertraversal
