# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. In C the bounds are tracked as `long` so `INT_MIN`/`INT_MAX` node values still validate.

## Run

    make && ./validatebst
