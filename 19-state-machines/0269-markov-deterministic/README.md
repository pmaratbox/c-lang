# 0269 — Deterministic Transition Walk

Walk a deterministic transition map A->B->C->A for three steps from A, printing the visited states `B C A`. In C, an `int next[]` table indexed by state encodes the map.

## Run

    make && ./markovdeterministic
