# 0264 — Turnstile FSM

Drive a turnstile (locked/unlocked) with events coin, push, push and print the resulting states `unlocked locked locked`. In C, a small `switch`/`if` transition function keeps the FSM explicit.

## Run

    make && ./turnstilefsm
