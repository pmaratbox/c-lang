# 0265 — Vending Machine

Insert coins 10, 10, 5 to reach the price 25 and dispense, printing `dispensed`. In C, accumulate an `int` total and branch on the threshold.

## Run

    make && ./vendingmachine
