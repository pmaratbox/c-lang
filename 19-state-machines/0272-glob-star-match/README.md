# 0272 — Glob Star Match

Match the glob "a*b" (* = any run) against "aaab" (yes) and "aac" (no), printing `yes no`. In C, a small recursive matcher backtracks on `*`.

## Run

    make && ./globstarmatch
