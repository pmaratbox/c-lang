# 0271 — DFA String Match

Build a DFA for the pattern "ab" and report the first match index in "aab", printing `1`. In C, the matched-prefix count acts as the DFA state as you scan the text.

## Run

    make && ./stringmatchdfa
