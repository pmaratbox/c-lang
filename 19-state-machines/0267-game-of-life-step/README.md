# 0267 — Game of Life Step

Advance one step of a vertical blinker on a 3x3 grid (it becomes horizontal); print the grid using . and #. In C, a fixed `int[3][3]` and a neighbor count keep the Conway rules concise.

## Run

    make && ./gameoflifestep
