# 0226 — Roman to Integer

Parse the Roman numeral "XIV" to its value `14`. In C a `switch` maps each letter to its value and we peek the next char with pointer arithmetic.

## Run

    make && ./romantoint
