# 0225 — Recursive Descent Calculator

Parse and evaluate "2+3*4" honoring precedence, printing `14`. In C a file-scope cursor pointer threads through the mutually simple parse functions.

## Run

    make && ./recursivedescentcalc
