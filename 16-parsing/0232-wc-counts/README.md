# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. In C we walk the string once, toggling an `in_word` flag and counting newlines.

## Run

    make && ./wccounts
