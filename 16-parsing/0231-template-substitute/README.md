# 0231 — Template Substitution

Substitute the variable in the template "hi {name}" with name="Ada", printing `hi Ada`. In C we scan for `{...}` spans with `strchr` and compare the key via `strncmp`.

## Run

    make && ./templatesubstitute
