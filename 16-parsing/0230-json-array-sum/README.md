# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. In C we accumulate digits and flush each number on any non-digit separator.

## Run

    make && ./jsonarraysum
