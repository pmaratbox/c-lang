# 0234 — Parse Number Bases

Parse "ff" as hexadecimal (255) and "101" as binary (5), printing `255 5`. In C `strtol` with an explicit base does the conversion for each string.

## Run

    make && ./numberbaseparse
